---
title: "MCP Gateway quickstart"
description: "Connect your first MCP client through the Portkey MCP Gateway."
---

<Info>
  MCP Gateway is available on [Portkey Cloud](https://app.portkey.ai). For the complete setup guide, see [portkey.ai/docs/product/mcp-gateway/quickstart](https://portkey.ai/docs/product/mcp-gateway/quickstart).
</Info>

## Overview

The MCP Gateway lets you connect any MCP-compatible client to your MCP servers through a single, authenticated endpoint hosted by Portkey. You configure which servers are accessible, define access policies, and every tool call is logged automatically.

## Prerequisites

- A [Portkey Cloud account](https://app.portkey.ai) (free tier available)
- At least one running MCP server
- An MCP-compatible client: Claude Desktop, Cursor, or VS Code

## Steps

<Steps>
  <Step title="Create a Portkey account">
    Sign up at [app.portkey.ai](https://app.portkey.ai). The free developer tier includes MCP Gateway access.
  </Step>

  <Step title="Register your MCP servers">
    In the Portkey dashboard, navigate to **MCP Gateway** and add your MCP server endpoints. Each server gets a unique name that clients use to identify it.

    You can register any publicly reachable MCP server, or use a tunneling tool like `ngrok` to expose a local server during development.
  </Step>

  <Step title="Generate a gateway URL and credentials">
    Portkey generates a unique MCP Gateway URL and API key for your workspace. Clients connect to this URL instead of connecting directly to your MCP servers.
  </Step>

  <Step title="Configure your MCP client">
    Update your MCP client to point at the Portkey MCP Gateway URL instead of each individual server.

    <CodeGroup>

    ```json Claude Desktop
    // ~/Library/Application Support/Claude/claude_desktop_config.json (macOS)
    // %APPDATA%\Claude\claude_desktop_config.json (Windows)
    {
      "mcpServers": {
        "portkey": {
          "url": "https://api.portkey.ai/mcp/YOUR_WORKSPACE_ID",
          "headers": {
            "x-portkey-api-key": "YOUR_PORTKEY_API_KEY"
          }
        }
      }
    }
    ```

    ```json Cursor
    // .cursor/mcp.json (project-level) or ~/.cursor/mcp.json (global)
    {
      "mcpServers": {
        "portkey": {
          "url": "https://api.portkey.ai/mcp/YOUR_WORKSPACE_ID",
          "headers": {
            "x-portkey-api-key": "YOUR_PORTKEY_API_KEY"
          }
        }
      }
    }
    ```

    ```json VS Code
    // .vscode/settings.json
    {
      "continue.mcpServers": [
        {
          "name": "portkey",
          "url": "https://api.portkey.ai/mcp/YOUR_WORKSPACE_ID",
          "headers": {
            "x-portkey-api-key": "YOUR_PORTKEY_API_KEY"
          }
        }
      ]
    }
    ```

    </CodeGroup>
  </Step>

  <Step title="Test with a tool call">
    Ask your AI assistant to use a tool from one of your registered MCP servers. The request flows through the Portkey MCP Gateway:

    1. Client sends the tool call to the Portkey MCP endpoint
    2. Gateway authenticates the request using your API key
    3. Gateway checks the access policy
    4. Gateway forwards the call to the appropriate MCP server
    5. Response is returned to the client
    6. The tool call is logged in your Portkey dashboard
  </Step>

  <Step title="View logs in the Portkey dashboard">
    Go to the **Logs** section in your Portkey dashboard to see all MCP tool calls — including who called what, with which parameters, the response, and latency.
  </Step>
</Steps>

## Next steps

<CardGroup cols={2}>
  <Card title="Authentication" icon="key" href="/mcp/authentication">
    Configure authentication methods for the gateway.
  </Card>
  <Card title="Access control" icon="shield" href="/mcp/access-control">
    Restrict which users and teams can access which servers and tools.
  </Card>
  <Card title="Observability" icon="eye" href="/mcp/observability">
    Explore logs, filtering, and integration with external tools.
  </Card>
  <Card title="Full documentation" icon="book" href="https://portkey.ai/docs/product/mcp-gateway">
    Complete MCP Gateway reference on portkey.ai.
  </Card>
</CardGroup>
